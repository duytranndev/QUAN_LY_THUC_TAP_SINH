<div class="mt-4">
    <div class="row">

        {{#each instructors}}

        <div class="col-md-3">
            <div class="card">
                <a href="/enterprises/{{this.id_enterprise}}/instructors/{{this.slug}}">
                    <img src="./assets/img/{{this.image}}" alt="">
                </a>
                <div class="card-body">
                    <a href="/enterprises/{{this.id_enterprise}}/instructors/{{this.slug}}">
                        <h5 class="card-title">{{this.title_post}}</h5>
                    </a>
                    <p class="card-text">{{this.describe_student}}</p>
                    <a href="/enterprises/{{this.id_enterprise}}/instructors/{{this.slug}}" class="btn btn-primary">go /:slug</a>
                </div>
            </div>
        </div>
        
        {{/each}}

    <a href="instructors/create" class="btn btn-success">them instructors</a>

    </div>
</div>